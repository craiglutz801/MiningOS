# Production build (Render / Vercel): feature tabs
VITE_ENABLE_TAX_SALES=true
# SITLA stays off in prod builds until ENABLE_SITLA_API is validated; set true on Render to enable.
VITE_ENABLE_SITLA=false
